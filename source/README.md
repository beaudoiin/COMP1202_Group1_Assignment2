New folder for project. all other documents outside of this is for reference only

Added some basic classes we should be using.

ColorConsole() //replaces Console,
AnyKeyToContinue() //replaces using the readKey for conformation, and printing a colored message
Strings //now replaced by messageOutput. In this case an Enum is declared, used as the key for a string in defualt langauge.
//That langauge is used when a language is missing, as defualt english.
//If no language loaded messageOutput = defaultEnglishMessages. This means messageOut contains english messages.

